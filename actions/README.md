# Actions

This tree stores action records, not raw knowledge.

Folders:
- `pending/`: staged tasks awaiting approval or execution
- `approved/`: approved tasks not yet executed
- `executed/`: immutable action outcome records
